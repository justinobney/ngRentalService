// This is the "global" controller for the application. Most funcitonality
// should be broken into more focused sections. I just normally include this
// incase i get into a time crunch and cant figure out a smart way to achieve
// needed functionality. In most cases, logic that you may put here will probably
// be better served in a service (read factory); 
// SEE: LayoutManager.js

(function () {
    'use strict';

    var app = angular.module('ngRentalService');

    app.controller('AppCtrl', [function () {
        
    }]);
})()